<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mad_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".faculty_class"/>
        <activity android:name=".faculty_quiz_class"/>
        <activity android:name=".set_quiz_question_class"/>
        <activity android:name=".student_class"/>
        <activity android:name=".student_quiz_class"/>
        <activity android:name=".new_rc"/>
        <activity android:name=".Main3456"/>


    </application>

</manifest>